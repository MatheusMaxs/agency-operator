# Deployer Service

MVP deployment exposes generated HTML previews through the API, or deploys static previews to Vercel when `VERCEL_TOKEN` is configured.

Future implementation can deploy previews to Cloudflare Pages, Vercel, Netlify, or static hosting on the VPS.
