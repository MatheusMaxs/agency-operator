# Auditor Service

MVP website auditing is implemented as a conservative HTTP/HTML heuristic in the API tool `audit_business`.

Future implementation should use Playwright, Lighthouse-style checks, screenshots, broken-link checks, SSL checks, and manual review signals.
