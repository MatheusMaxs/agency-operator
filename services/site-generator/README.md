# Site Generator Service

MVP site generation creates a legal preview from public business fields and can use NVIDIA build text models for the design brief when configured.

Future implementation should add component libraries, licensed imagery, design-reference extraction, screenshots, and build checks.
